fixer:
  url: https://data.fixer.io/api
  api-key: ${FIXER_API_KEY}
collector:
  interval: 3600000
  base-currency: USD
  target-currencies:
    - EUR
    - GBP
    - JPY
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/currency_gateway
    username: ${DB_USERNAME:currency}
    password: ${DB_PASSWORD:currency}
    hikari:
      pool-name: CurrencyGatewayHikariPool
      maximum-pool-size: 20
      minimum-idle: 5
      idle-timeout: 600000
      max-lifetime: 1800000
      connection-timeout: 30000
      validation-timeout: 5000
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
  rabbitmq:
    host: ${RABBITMQ_HOST:localhost}
    port: ${RABBITMQ_PORT:5672}
    username: ${RABBITMQ_USERNAME:guest}
    password: ${RABBITMQ_PASSWORD:guest}
    template:
      exchange: ${RABBITMQ_EXCHANGE:currency.statistics}
